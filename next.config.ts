import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* 
    Note: 'eslint' and 'output: export' are removed for Next.js 16 compatibility 
    and to ensure NextAuth/Database features work correctly on Vercel.
  */
};

export default nextConfig;
